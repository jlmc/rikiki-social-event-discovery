{
  "name": "rikiki-mobile-app",
  "version": "1.0.0",
  "private": true,
  "description": "On-device (no server, no disk writes) Expo/React Native app that searches the same real cultural/family events as the CLI in ../cli.",
  "main": "index.js",
  "scripts": {
    "start": "expo start",
    "web": "expo start --web"
  },
  "dependencies": {
    "expo": "~51.0.0",
    "expo-status-bar": "~1.12.1",
    "react": "18.2.0",
    "react-native": "0.74.5",
    "node-html-parser": "^6.1.13",
    "@react-native-async-storage/async-storage": "1.23.1"
  },
  "devDependencies": {
    "babel-preset-expo": "~11.0.0",
    "@babel/core": "^7.24.0"
  }
}
